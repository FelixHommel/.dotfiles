[core]
    pager = diff-so-fancy | less --tabs=4 -RF

[init]
	defaultBranch = main

[interactive]
    diffFilter = diff-so-fancy --patch

[diff]
    context = 3
    renames = copies
    interHunkContext = 10

[diff-so-fancy]
	stripLeadingSymbols = false

[color]
	ui = true
